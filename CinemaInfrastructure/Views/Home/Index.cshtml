@{
    ViewData["Title"] = "Головна сторінка";
    var navLinks = ViewData["NavLinks"] as List<(string Text, string Controller, string Action)>;
}

<div class="d-flex align-items-center justify-content-center mb-4">

    <!-- Навігація праворуч -->
    <nav class="nav">
        <a class="nav-link text-light" href="@Url.Action("Index", "FilmCategories")">Категорії фільмів</a>
        <a class="nav-link text-light" href="@Url.Action("Index", "Companies")">Виробники фільмів</a>
        <a class="nav-link text-light" href="@Url.Action("Index", "HallTypes")">Типи залів</a>
        <a class="nav-link text-light" href="@Url.Action("Index", "Films")">Фільми</a>
        <a class="nav-link text-light" href="@Url.Action("Index", "Halls")">Зали</a>
        <a class="nav-link text-light" href="@Url.Action("Index", "Seats")">Місця</a>
        <a class="nav-link text-light" href="@Url.Action("Index", "Sessions")">Сеанси</a>
        <a class="nav-link text-light" href="@Url.Action("Index", "Viewers")">Глядачі</a>
        <a class="nav-link text-light" href="@Url.Action("Index", "FilmRatings")">Оцінки</a>
        <a class="nav-link text-light" href="@Url.Action("Index", "Bookings")">Бронювання</a>
    </nav>
</div>

<div class="text-center">
    <h1 class="display-3">DreamScreen</h1>
</div>

<!-- Обгортка, що вирівнює все в один рядок по центру -->

<div class="row">
    <div class="col-6">
        <div id="countByType"></div>
    </div>
    <div class="col-6">
        <div id="countByHalls"></div>
    </div>
</div>

@section Scripts {
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
        google.charts.load('current', {'packages':['corechart']});
        google.charts.setOnLoadCallback(drawCharts);

        function drawCharts() {
            // Діаграма №1 (за жанрами)
            fetch('/api/values/countByType')
                .then(response => response.json())
                .then(data => {
                    const dataTable = new google.visualization.DataTable();
                    dataTable.addColumn('string', 'Жанр');
                    dataTable.addColumn('number', 'Кількість фільмів');

                    data.forEach(item => {
                        dataTable.addRow([item.cinemaType, item.count]);
                    });

                    const options = {
                        title: 'Фільми за жанрами',
                        titleTextStyle: {
                            color: '#0be7fb',
                            fontSize: 18
                        },
                        hAxis: { textStyle: { color: '#0be7fb' } },
                        vAxis: { textStyle: { color: '#0be7fb' } },
                        legend: { textStyle: { color: '#0be7fb' } },
                        width: 600,
                        height: 400,
                        legend: { position: 'none' },
                        backgroundColor: 'transparent',
                        colors: ['#0be7fb']
                    };

                    const chart = new google.visualization.ColumnChart(document.getElementById('countByType'));
                    chart.draw(dataTable, options);
                })
                .catch(error => {
                    console.error('Error loading chart data (countByType):', error);
                });

            // Діаграма №2 (за залами)
            fetch('/api/values/countByHalls')
                .then(response => response.json())
                .then(data => {
                    const dataTable = new google.visualization.DataTable();
                    dataTable.addColumn('string', 'Зал');
                    dataTable.addColumn('number', 'Кількість сеансів');

                    data.forEach(item => {
                        dataTable.addRow([item.cinemaType, item.count]);
                    });

                    const options = {
                        title: 'Сеанси за залами',
                        titleTextStyle: {
                            color: '#0be7fb',
                            fontSize: 18
                        },
                        hAxis: { textStyle: { color: '#0be7fb' } },
                        vAxis: { textStyle: { color: '#0be7fb' } },
                        legend: { textStyle: { color: '#0be7fb' } },
                        width: 600,
                        height: 400,
                        legend: { position: 'none' },
                        backgroundColor: 'transparent',
                        colors: ['#0be7fb']
                    };

                    const chart = new google.visualization.ColumnChart(document.getElementById('countByHalls'));
                    chart.draw(dataTable, options);
                })
                .catch(error => {
                    console.error('Error loading chart data (countByHalls):', error);
                });
        }
    </script>
}

